# Production CUDA 12.6 dependency family for MoE LoRA/FSDP training.
# Install torch from the official PyTorch CUDA wheel index, then install the project with this constraints file.
# Example: python -m pip install --index-url https://download.pytorch.org/whl/cu126 "torch==2.7.1"
# Example: python -m pip install -c constraints/cuda126_kernels.txt -e ".[cuda-kernels]"

# HF/PEFT/FSDP stack
accelerate>=1.14,<2
peft>=0.19.1,<0.20
transformers>=5.12,<6

# CUDA extension build/import stack
nvidia-cuda-runtime-cu12==12.6.77
causal-conv1d==1.6.2.post1
flash-attn==2.8.3.post1
ninja>=1.11,<2
packaging>=24

# Keep torch in the CUDA-12.x compatible stable family; install the actual CUDA wheel explicitly.
torch>=2.7,<2.13
