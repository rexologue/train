extends: smoke.yaml

project:
  name: qwen35-a3b-sft
  run_name: null
  output_dir: runs/qwen35-a3b-sft

training:
  max_steps: 10000
  gradient_accumulation_steps: 16
  logging_steps: 5
  eval_steps: 100
  checkpoint_steps: 100
  save_total_limit: 5

